// Central API base URL used by all frontend calls
// Override window.API_BASE in production (e.g., set to your backend URL)
window.API_BASE = window.API_BASE || `${location.protocol}//${location.hostname}:3000`;
