{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open with Live Server",
            "url": "http://localhost:8080/unravel-static/loading.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}